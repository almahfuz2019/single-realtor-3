/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.ibb.co"],
    unoptimized: true,
  },
  // useFileSystemPublicRoutes: false,
  // output: "export",
};

export default nextConfig;
